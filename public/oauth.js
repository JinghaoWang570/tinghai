function oauthButton(){const o=state.oauth;if(!o)return '<button class="oauth-button" data-action="oauth-status">知乎登录</button>';return o.authenticated?'<button class="oauth-button connected" data-action="oauth-logout">知乎已连接</button>':`<button class="oauth-button" data-action="oauth-login">${o.configured?'知乎登录':'登录待配置'}</button>`}
function oauthAvatar(){try{const url=new URL(state.oauth?.profile?.avatarUrl||'');return url.protocol==='https:'&&/(^|\.)zhimg\.com$/i.test(url.hostname)?url.href:''}catch{return ''}}
async function refreshOauth(){try{state.oauth=await request('/api/oauth/status');render();if(state.oauth.authenticated&&!state.oauth.profile)request('/api/oauth/profile').then(o=>{if(state.oauth?.authenticated){state.oauth=o;render()}}).catch(()=>{});if(typeof collectionState!=='undefined'&&state.oauth.authenticated&&collectionState.lists===null)loadCollections()}catch{if(location.protocol==='https:'){state.oauth={enabled:true,configured:true,authenticated:false,notice:'登录状态暂时未同步，可稍后重试。'};render()}}}
async function oauthLogin(){if(!state.oauth?.configured){showDialog('知乎账号登录',`<div class="oauth-sheet"><span class="oauth-logo">知</span><h2>登录能力已经接入</h2><p>本地地址只能预览界面。配置 App ID、App Key 和部署后的 HTTPS 回调地址后，即可从这里跳转到知乎授权。</p><p class="micro">${esc(state.oauth?.notice||'真实授权等待部署配置。')}</p></div>`);return}window.top.location.assign('/auth/login')}
function oauthLogout(){window.top.location.assign('/auth/logout')}
function oauthStatusDialog(){const o=state.oauth;showDialog('知乎账号登录',`<div class="oauth-sheet"><span class="oauth-logo">知</span><h2>${o?.authenticated?'知乎账号已连接':o?.configured?'可以登录知乎':'等待部署配置'}</h2><p>${o?.authenticated?'“我的知乎”现在读取当前授权账号的公开创作、关注和收藏。':o?.configured?'点击右上角“知乎登录”开始授权。':'需要配置知乎 OAuth 应用，并使用公网 HTTPS 回调地址。'}</p><p class="micro">${esc(o?.notice||'')}</p></div>`)}
document.addEventListener('click',e=>{const b=e.target.closest('button');if(!b)return;if(b.dataset.action==='oauth-login')oauthLogin();if(b.dataset.action==='oauth-logout')oauthLogout();if(b.dataset.action==='oauth-status')oauthStatusDialog()});
refreshOauth();
